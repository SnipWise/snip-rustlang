FROM rust:1.85-slim

WORKDIR /playground

# Install useful tools
RUN apt-get update && apt-get install -y \
    git \
    vim \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Configure fancy prompt
RUN echo 'export PS1="\[\033[1;36m\]🦀 rust-playground\[\033[0m\] \[\033[1;33m\]\w\[\033[0m\] \[\033[1;32m\]>\[\033[0m\] "' >> /etc/bash.bashrc

# Keep container running
CMD ["tail", "-f", "/dev/null"]
